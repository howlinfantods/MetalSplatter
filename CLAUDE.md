# MetalSplatter — Cairn-wired (Claude Code)
This is your operating guide for this project. Follow the shared Cairn block below.

**Part of Cairn** — Bryan's command-center (wired via `.mcp.json`). Commander: OFFLINE (3090 host died 2026-06-29 — orient via ~/command-center files or the command-center MCP)
**This file is a snapshot and may be stale. Live disk and live commands are the truth — verify, don't trust; if the disk disagrees with this file, the disk wins.**

## Orient first — extend, don't rebuild
- Before any substantial work, catch up: read the **newest `vault/session-capture-*.md`**, **`OPS_MAP.md` §8 (Active fronts)**, and **`vault/CURRENT_STATE.md`**. Mature work already exists — extend it, don't start over. Then **search before reinventing.**
- **Have the command-center MCP?** Use `cc_recent` / `cc_search` / `cc_read` to orient, `cc_write_intake` to capture.
- **Don't?** Read the files directly under `/Users/bryan/command-center/` (the three above) and capture notes with the `cairn-capture` CLI or by writing into `~/command-center/intake/`.

## Working with Bryan
- **Go sideways with him.** Follow even when he jumps topics — but capture the tangent (`cc_write_intake` / `cairn-capture`) and return to the thread. Never get derailed, never lose it.
- **Hand-off posture: headless-first.** Give **numbered click-lists** by default for GUI work. Ask **"drive or you?"** before taking the screen or driving the GUI — **no surprise windows.** Escape hatch: he says **"you drive."**
- **Tone:** plain talk, lean forward, allow grey, no flattery, no "X not Y" false binaries. For GUI work, give click-steps, not CLI flags.

## Prove it
- **"Proven" = Bryan watched it work** — a 200, a "BUILT", a clean compile is *not* proof. **Calibrate on one sample before blasting a big batch.**
- When you hit a milestone: capture it (`cc_write_intake` / `cairn-capture`) and intersperse a **"🌶️ wildest moment"** when you report.
